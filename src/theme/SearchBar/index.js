import React from 'react';
import SearchBar from '../../components/SearchBar';

// This is the swizzled SearchBar component that Docusaurus will use
// for both desktop and mobile navigation automatically
export default function CustomSearchBar() {
  return <SearchBar />;
}
